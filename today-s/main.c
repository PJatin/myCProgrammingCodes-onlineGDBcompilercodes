#include <stdio.h>
#include<string.h>
int main()
{
    int i,j,k;
    char str[200],b[100];
    scanf("%[^\n]%*c",str);
    printf("%s\n",str);
    for(i=0;str[i]!='\0';i++)
    {
        if(str[i]!=' ')
        {
            b[k][j]=str[i];
            j++;
        }
        else
        {
            b[k][j]=
            while(k!=-1)
            {
                printf("%c",str[k]);
                k--;
            }
            k=0;
        }
    }
    return 0;
}
